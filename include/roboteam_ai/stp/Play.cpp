//
// Created by john on 3/9/20.
//

#include "Play.hpp"

namespace rtt::ai::stp {
    Status Play::update(const rtt::ai::stp::TacticInfo &info) {
        size_t finishedCount = 0;
        size_t waitingCount = 0;
        size_t
        for (auto const& each : )

    }
}
